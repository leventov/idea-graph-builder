To run main.py, set env varaibles:

 - OPENAI_API_KEY (GPT-4 access requeried),
 - TOKENS_PER_MIN_LIMIT (likely 10000 unless you have contacted OpenAI about a larger limit)
 - REQUESTS_PER_MIN_LIMIT (likely 200).